/// <reference types="node" />
import { EventEmitter } from 'events';
import { MailerConfig, Metrics, MailOptions, SendResult } from '../';
declare class FastMailer extends EventEmitter {
    private config;
    private socket;
    private connectionPool;
    private metrics;
    private logFilePath;
    private rateLimits;
    constructor(config: MailerConfig);
    private validateEmail;
    private checkRateLimit;
    private createConnection;
    private sendCommand;
    private generateBoundary;
    private sanitizeHeader;
    private sanitizePath;
    private buildMimeMessage;
    private detectMimeType;
    private shouldLog;
    private maskSensitiveData;
    private writeLog;
    sendMail(options: MailOptions): Promise<SendResult>;
    private updateMetrics;
    getMetrics(): Metrics;
    verifyConnection(): Promise<boolean>;
}
export default FastMailer;
